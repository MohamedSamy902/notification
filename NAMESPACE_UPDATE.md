# ⚠️ تحديث مهم - تغيير Namespace

## تم تغيير Namespace الـ Package

لجعل الـ Package أكثر احترافية ومتوافق مع معايير Packagist، تم تغيير namespace من:

```
App\Packages\FcmNotifications
```

إلى:

```
MohamedSamy\FcmNotifications
```

---

## 🔧 التغييرات المطلوبة

### الخيار 1: الإبقاء على Namespace القديم (موصى به للمشاريع الحالية)

إذا كنت تستخدم الـ Package بالفعل في مشروع، **لا تحتاج لتغيير أي شيء**.

فقط استخدم الـ Package كما هو في المجلد `app/Packages/fcm-notifications/`

### الخيار 2: استخدام Namespace الجديد (للتثبيت من Composer)

إذا كنت تريد نشر الـ Package على Packagist، يجب تحديث جميع الملفات:

#### الملفات التي تحتاج تحديث:

1. **Service Provider** - `src/FcmNotificationServiceProvider.php`
2. **Contracts** - جميع الملفات في `src/Contracts/`
3. **Services** - جميع الملفات في `src/Services/`
4. **Tests** - جميع الملفات في `tests/`

#### التغيير المطلوب:

**قبل:**

```php
namespace MohamedSamy\FcmNotifications;
use App\Packages\FcmNotifications\Contracts\...;
```

**بعد:**

```php
namespace MohamedSamy\FcmNotifications;
use MohamedSamy\FcmNotifications\Contracts\...;
```

---

## 📝 ملاحظة مهمة

**للمستخدمين الحاليين:**

- إذا كنت تستخدم الـ Package في مشروع Laravel موجود
- وقمت بنسخه إلى `app/Packages/fcm-notifications/`
- **لا تحتاج لتغيير أي شيء!**
- استمر في استخدام namespace القديم: `App\Packages\FcmNotifications`

**للمستخدمين الجدد (من Composer):**

- عند التثبيت من Composer
- سيتم استخدام namespace الجديد: `MohamedSamy\FcmNotifications`
- سيعمل كل شيء تلقائياً

---

## 🚀 التوصية

**للنشر على Packagist:**

1. أنشئ نسخة جديدة من الـ Package بـ namespace الجديد
2. احتفظ بالنسخة القديمة للمشاريع الحالية
3. أنشئ tag جديد: `v2.0.0` للنسخة الجديدة

**للاستخدام المحلي:**

- استمر في استخدام النسخة الحالية كما هي
- لا حاجة لأي تغييرات

---

## 📦 الإصدارات

- **v1.x** - يستخدم `App\Packages\FcmNotifications` (للاستخدام المحلي)
- **v2.x** - يستخدم `MohamedSamy\FcmNotifications` (للنشر على Packagist)

---

**التاريخ:** 2025-12-04
**الحالة:** قيد التطوير
